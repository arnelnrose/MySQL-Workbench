DISTINCT KEYWORD
The Distinct keyword is used to select UNIQUE
values only from the parameters provided.

Example:
Selecting employees address without any repeating Data.

QUERY:

Example #1
SELECT LastName FROM tblemployees;

Example #1
SELECT DISTINCT LastName FROM tblemployees;